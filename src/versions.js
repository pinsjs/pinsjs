const pinVersionsPathName = () => {
  throw 'NYI';
};

const pinVersionSignature = (hashFiles) => {
  throw 'NYI';
};

const pinVersionsPath = (storagePath) => {
  throw 'NYI';
};

export const boardVersionsEnabled = ({ versions }) => versions === true;

const boardVersionsCreate = (board, name, path) => {
  throw 'NYI';
};

const boardVersionsGet = (board, name) => {
  throw 'NYI';
};

const boardVersionsShorten = (versions) => {
  throw 'NYI';
};

const boardVersionsExpand = (versions, version) => {
  throw 'NYI';
};
